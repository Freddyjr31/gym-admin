name: gym_admin
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+0006

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
      sdk: flutter
  fluent_ui: ^4.15.0
  hive:
  hive_flutter:
  msix: ^3.16.13
  package_info_plus: ^9.0.0
  provider: ^6.1.5+1


dev_dependencies:
  flutter_test:
      sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator:
  build_runner:
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
