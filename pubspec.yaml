name: gym_admin
description: >
  Aplicación de escritorio para Windows desarrollada en Flutter.
  Permite gestionar recetas, ingredientes y costos de producción,
  con una interfaz moderna basada en Fluent UI.

publish_to: 'none'
version: 0.1.0+0010

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
      sdk: flutter
  fluent_ui: ^4.15.0
  hive:
  hive_flutter:
  msix: ^3.16.13
  package_info_plus: ^9.0.0
  provider: ^6.1.5+1


dev_dependencies:
  flutter_test:
      sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator:
  build_runner:
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
